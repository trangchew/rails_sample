build:
  phpunit:
    image: dangminhtruong/rails_base_with_ci
    commands:
    - cp config/database-ci.yml config/database.yml
    - bundle install
    - RAILS_ENV=test rake db:create db:migrate
    - framgia-ci run --local
services:
  mysql:
    image: mysql:5.7
    environment:
      MYSQL_DATABASE: ci_test
      MYSQL_USER: test
      MYSQL_PASSWORD: test
      MYSQL_ROOT_PASSWORD: root